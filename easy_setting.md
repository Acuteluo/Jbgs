# 快速配置（相机/红外/传感器 绑定向导）

首次绑定或换机后运行向导：

```bash
cd <工程根>
source env/env.sh
ros2 run galaxy_camera_dual camera_setup_wizard
```

只检查不写入（验收用）：

```bash
ros2 run galaxy_camera_dual camera_setup_wizard --check
```

## 向导行为约定（2026-09 多轮开箱实测）

1. **网卡不用手抄**：向导按 SDK 枚举报告的网卡 MAC 自动反查相机所在网口（枚举打印会带"(所在网卡 xxx)"）；全新机器枚举为空时才需要按提示人工选网卡编号（先用 `ip -br link` 确认哪个口插着相机交换机）。
2. **首地址规则**：GxGVTL 以网卡"首个全局 IPv4"为控制通道源地址。相机网段地址（LLA 时 `169.254.100.1/16`）必须排在该网卡首位，否则能枚举但 open 超时 -14。向导自动保证并会把被弄乱的顺序修回来。
3. **与 mid360 共用交换机/网卡没问题**：重排地址时 mid360 地址原序保留（闪断约 1 秒）。mid360 的静态地址（`192.168.1.50/24` + 雷达 `192.168.1.161`，导航侧配置）不归向导管；重装系统后需手工补回。
4. **预览确认**：全屏预览按窗口 ENTER 结束；Wayland/SSH 下窗口拿不到焦点时，**终端直接回车**等效，输入 `s` 回车=跳过。帧数 <5 时确认被拦截，防止把无流相机绑进配置。
5. **USB 串口打不开**（新装系统常见）：brltty 会抢占 CH340，ModemManager 也会占口——`sudo systemctl mask --now brltty brltty-udev ModemManager` 后重新插拔 USB。
6. **扩展坞（USB 千兆网卡，RTL8153/r8152）接交换机**：向导会先打印识别结果（USB 2.0 坞 / USB 3.x 坞 / 板载直插网口），再双机实测，最后打印写入的上限（帧率、包长、MTU、每台限速）。**务必把坞插到 USB3/USB4 口**——插在 USB2（lsusb -t 显示 480M）时双 5MP 会打满总线，向导会明确提示换口而不会写入残帧配置。USB3/板载千兆可到 8–10 fps。红外/传感器可以继续走扩展坞 USB；只有交换机改插板载网口时，向导必须认板载口，并清掉坞以太网口上残留的 169.254 地址（USB 设备不受影响）。换口/换线后可只重测：
   ```bash
   ros2 run galaxy_camera_dual camera_setup_wizard --tune-fps
   ```
   向导对 NM 托管的网卡把配置写进连接（相机网段地址必须排首位，mid360 的 `192.168.1.50/24` 原样保留）；未托管时装 `jbgs-gige-<网卡>.service`（会一并写回 mid360 地址，禁止只留相机地址）。
   - 注意：该服务会在坞出现时 flush 网口全局地址再按「相机地址 + 原有其它地址」加回——**只适用于专接相机交换机的扩展坞**；若扩展坞网口要兼作他网（如上外网），不要装/要卸载该服务。
   - 换到笔记本电脑上使用：笔记本需先装好本工程（colcon 构建）与 GalaxySDK（仓库自带），向导逻辑与机器无关（网卡名/JBGS_ROOT 全动态探测），同流程运行即可。
7. **持久化**：网络配置写入 NetworkManager 连接（manual + MTU 9000 + 相机地址首位 + autoconnect），重启自动恢复，无需再跑向导。

## 配置完成后真机运行

```bash
./run.sh sim:=false
```

传感器未确认时 `sim_mode` 保持 true，接好传感器后重跑一次向导补确认即可。
